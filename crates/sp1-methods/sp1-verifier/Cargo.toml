[package]
name = "sp1-verifier"
version = "0.1.0"
authors = ["Automata Team"]
edition = "2021"
license = "Apache-2.0"

[workspace]

[dependencies]
sp1-zkvm = "^5.0.0"
aws-nitro-enclave-attestation-verifier = { path = "../../verifier", features = ["sp1"] }
