[package]
name = "risc0-verifier"
version = "0.1.0"
authors = ["Automata Team"]
edition = "2021"
license = "Apache-2.0"

[workspace]

[dependencies]
risc0-zkvm = { version = "^2.2.0", default-features = false, features = ["std"] }
aws-nitro-enclave-attestation-verifier = { path = "../../verifier", features = ["risc0"] }
