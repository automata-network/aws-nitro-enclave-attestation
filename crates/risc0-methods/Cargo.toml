[package]
name = "risc0-methods"
version = "0.1.0"
edition = "2021"

[dependencies]
lazy_static = { workspace = true }
risc0-zkvm = { workspace = true }

[build-dependencies]
risc0-build = "=2.2.0"

[package.metadata.risc0]
methods = ["risc0-verifier", "risc0-aggregator"]
